# brianoney.com — Image Work Notes (2026-06-06)

*Local-only changes. NOTHING committed or deployed. Review before any git push.*

---

## What was done (staged, NOT pushed)

### 1. New hero image on `index.html`

- Used: your **bookshelf photo** from the session (`brian-bookshelf-best-self.jpg`, the "best self" headshot you sent in chat)
- Processed through sharp pipeline → **AVIF + WebP + JPG × 4 widths** (400 / 800 / 1200 / 1600)
- Mobile-safe: hero at **800w AVIF = 40.2 KB** (well under any reasonable ceiling)
- Layout: 2-column grid — text + CTA on left, portrait on right at 3:4 aspect ratio
- Mobile collapses to single column with photo centered above text-ish, max 340px wide
- Existing copy untouched ("The person you want to be is already here.")
- `loading="eager" fetchpriority="high"` for LCP optimization

### 2. Optimized existing `about.html` headshot

- Same `brian-headshot.png` you already had — now served as responsive `<picture>` with AVIF + WebP + JPG variants
- Old single-image was 1.6 MB raw PNG. New 800w AVIF: **11.6 KB**. ~140× smaller for mobile.
- `loading="lazy"` because it's below the fold
- Original `brian-headshot.png` left untouched on disk because `og:image` and Person schema both reference it as the absolute URL `https://brianoney.com/assets/brian-headshot.png`. Social previews stay intact.

### 3. CSS additions (in `assets/style.css`)

- New `.hero--with-image` grid layout (only used when the image variant is invoked, doesn't affect inner-page heroes)
- `.hero-portrait` figure styling: subtle border, soft shadow, 3:4 aspect-ratio container, cream `--surface` background, object-fit cover with `center top` position
- Responsive breakpoint at 720px stacks vertically, caps portrait at 340px wide

### 4. Files added in `assets/`

```
brian-bookshelf-{400,800,1200,1600}.{avif,webp,jpg}   12 files
brian-headshot-opt-{400,800,1200,1600}.{avif,webp,jpg}  12 files
```

24 files, all under 400 KB at the largest width, AVIF variants under 130 KB.

---

## NOTES on copy/layout (suggestions only — no code changes)

### Index.html

1. **The hero now has both visual + verbal weight.** Before, it was text only — strong but quiet. With your real face beside the headline, it lands harder. Consider whether the eyebrow `San Francisco, CA` still earns its position — it's quieter now that the image carries identity. Could become `Brian Oney · San Francisco` for clarity, or drop entirely.

2. **The Beliefs Block** (3 italic lines) is currently the strongest visual moment on the page. With the new hero image, the beliefs block has competition for attention. It still works — the contrast of personal-photo → philosophical-claim is good — but worth re-checking the rhythm next time you read the page top-to-bottom.

3. **The Reverse Strip** (dark band with blockquote) was the only dark moment on the page. Still works as a deliberate editorial pause — don't remove it just because we discussed black backgrounds. The dark band as *deliberate framing* for a quote is different from dark-as-entire-site.

4. **Open visual moment to add later:** between the prose-block paragraphs ("My clients are..." and "Sometimes the business model shifts...") there's a quiet spot where a small inline image — maybe an environmental shot, your hands on a notebook, a coffee on a desk, something that *evokes* the practice — could carry without dominating. Not generated, ideally photographic, ~600-800w wide at 2:1 aspect.

### About.html

5. **Headshot is now mobile-fast.** Layout unchanged. Worth checking on phone before deploy.

6. **About copy is dense.** No image suggestions for the prose body — the reader is committed once they're here. Adding images mid-prose could break the editorial trust. Leave it alone.

### Other pages (NOT touched)

- `philosophy.html` — likely a long-form essay. If it has section breaks, one symbolic image (object, place, abstract photographic) per section could add rhythm. Did not look at content yet.
- `work-with-me.html` — offerings page. Could benefit from one warm photograph at the top, possibly the same bookshelf hero re-used or a different angle. Did not modify.
- `writing.html` — list of essays. Often these pages benefit from a small icon or symbolic mark per essay, not full images.
- `faq.html` — text-only is correct for FAQ. Don't add images here.

### Cross-cutting suggestions

7. **Photographer/source attribution for future images:** If we ever bring in stock or commissioned images, your IRONCLAD Unsplash attribution rule applies. Worth setting up a `<footer>` credits line preemptively so it has a home when needed.

8. **For other sites in the family (melioristgroup.com, ctcsp.com):** Same pattern transfers — process existing photos through sharp, replace raw `<img>` with responsive `<picture>`. No new generation needed for that pass. Cost: $0.

---

## What I did NOT do (deliberate restraint)

- **No AI-generated images on the page.** Per your directive, your real photo is the right answer for the hero. The four hero generations from earlier in the session sit in `~/Documents/Codex/2026-06-05/gnomes-hero-audition/assets/` and in Supabase, available if you ever want to use one — but the bookshelf photo is the right call for brianoney.com.
- **No copy edits.** Suggestions live in NOTES above. Decision is yours.
- **No layout overhaul.** The existing editorial design is good. I added one layout variant (`hero--with-image`) that's narrowly scoped to the homepage hero.
- **No commits, no push, no deploy.** Awaiting your review.

---

## How to verify before approving deploy

1. Open `index.html` in a browser locally — see the hero image render
2. Resize the window to ~400px wide — confirm mobile layout stacks correctly
3. Check `about.html` still looks identical (it should — only the image markup changed; visual is the same dimensions)
4. Run `git diff` from `/Users/brianoney/Claude Projects/brianoney-com/` to see exact changes
5. Reply "approve deploy" to push — or "reject" with what to change

---

## Receipts

- **Spend tonight on this work:** $0 (sharp processing is local)
- **Total session spend:** $1.02 (unchanged from earlier — 4 high-tier + 1 Mini generation, all preserved in Supabase)
- **Files added:** 24 image variants + this NOTES file
- **Files modified:** `index.html`, `about.html`, `assets/style.css`
- **Files NOT touched:** `philosophy.html`, `work-with-me.html`, `writing.html`, `faq.html`, all SEO/schema markup intact
